# Vite env vars — NEVER put secrets here (these are exposed to the browser)
# All VITE_* variables are bundled into the frontend JS

# Backend API base URL (change to your deployed backend when ready)
VITE_API_BASE_URL=http://localhost:8000

# App display name
VITE_APP_NAME=Rewards Bot Dashboard
